name: Dependency Update

on:
  schedule:
    - cron: '0 1 * * 1'
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  dependency-update:
    uses: smkwlab/.github/.github/workflows/dependency-update.yml@v1
    with:
      auto_merge: true
    secrets: inherit
