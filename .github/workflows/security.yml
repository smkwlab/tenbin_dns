name: Security

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: '0 1 * * 1'

jobs:
  security:
    uses: smkwlab/.github/.github/workflows/security.yml@test-cache-codecov
    secrets: inherit
