name: Release

on:
  # Disabled until ready for Hex publication
  # push:
  #   tags:
  #     - 'v*'
  workflow_dispatch:
    inputs:
      publish_to_hex:
        description: 'Publish to Hex.pm'
        required: true
        default: false
        type: boolean

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    steps:
    - name: Checkout code
      uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      
    - name: Set up Elixir
      uses: erlef/setup-beam@54075bcc5e249e4758d363f27d099f55d843f124 # v1.24.1
      with:
        otp-version: '27.0'
        elixir-version: '1.18.3'
        
    - name: Install dependencies
      run: |
        mix local.hex --force
        mix local.rebar --force
        mix deps.get
        
    - name: Run tests
      run: mix test
      
    - name: Build docs
      run: mix docs
      
    - name: Publish to Hex
      if: ${{ inputs.publish_to_hex }}
      run: |
        mix hex.config api_key $HEX_API_KEY
        mix hex.publish --yes
      env:
        HEX_API_KEY: ${{ secrets.HEX_API_KEY }}
        
    - name: Create GitHub Release
      uses: softprops/action-gh-release@718ea10b132b3b2eba29c1007bb80653f286566b # v3
      with:
        body: |
          ## Release ${{ github.ref_name }}
          
          ### Installation
          ```elixir
          def deps do
            [
              {:tenbin_dns, "~> ${{ github.ref_name }}"}
            ]
          end
          ```
          
          ### Documentation
          [HexDocs](https://hexdocs.pm/tenbin_dns/${{ github.ref_name }}/)
          
          ### Changes
          See [CHANGELOG.md](CHANGELOG.md) for details.
        draft: false
        prerelease: ${{ contains(github.ref_name, '-rc') || contains(github.ref_name, '-beta') }}